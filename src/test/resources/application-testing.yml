# env vars values loaded from: application-testing.env, via: spring-dotenv
spring:
  ssl:
    bundle:
      pem: {}
server:
  port: 9090
logging:
  level:
    org.springframework: DEBUG
management:
  endpoints:
    web:
      exposure:
        include: health

